# Backend API URL — relative path goes through Caddy reverse proxy
VITE_API_URL=/api

# Set to "true" to bypass DYMO Connect (no-op print, healthcheck always enabled).
# Useful for local testing on machines without a DYMO printer / web service.
# VITE_FAKE_DYMO=true
