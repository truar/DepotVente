{
    auto_https off
}

:15174 {
    tls /etc/caddy/certs/cert.pem /etc/caddy/certs/cert-key.pem

    handle /api/* {
        reverse_proxy host.docker.internal:3000
    }

    handle {
        reverse_proxy host.docker.internal:15173
    }
}
